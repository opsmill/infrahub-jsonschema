[project]
name = "infrahub-schema"
version = "0.1.0"
description = "Infrahub JSON Schema validation tools"
requires-python = ">=3.9,<3.15"
dependencies = [
    "invoke>=2.1.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "jsonschema>=4.17.0",
]
